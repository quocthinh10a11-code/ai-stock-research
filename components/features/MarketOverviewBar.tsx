import { marketIndices } from "@/lib/mock-data";
export function MarketOverviewBar() { return <div className="grid border-y border-slate-200 bg-white sm:grid-cols-2 xl:grid-cols-5"><div className="flex items-center px-4 py-3"><div><p className="label">Market status · placeholder</p><p className="mt-1 font-display text-sm font-semibold text-bull">HOSE · Open</p></div></div>{marketIndices.map(i => <div key={i.name} className="border-l border-slate-200 px-4 py-3"><p className="label">{i.name} · demo</p><div className="mt-1 flex items-baseline gap-2"><span className="data font-semibold">{i.value}</span><span className={`data text-xs ${i.change >= 0 ? "text-bull" : "text-bear"}`}>{i.change >= 0 ? "+" : ""}{i.change}%</span></div></div>)}</div>; }
