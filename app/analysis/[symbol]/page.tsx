import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { notFound } from "next/navigation";
import { getAnalysis } from "@/lib/data/get-analysis";
import { TopNavBar } from "@/components/ui/TopNavBar";
export default async function AnalysisPage({params}:{params:Promise<{symbol:string}>}) { const {symbol}=await params; await new Promise((resolve) => setTimeout(resolve, 1200)); const item=await getAnalysis(symbol); if (!item) notFound(); return <><TopNavBar variant="public"/><main className="min-h-screen bg-canvas px-5 pb-20 pt-28"><div className="mx-auto max-w-5xl"><Link href="/home" className="inline-flex items-center gap-2 text-sm text-slate-500"><ArrowLeft size={15}/>Back to market</Link><div className="mt-8 border border-slate-200 bg-white p-6 md:p-8"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="font-mono text-sm font-semibold text-slate-500">{item.symbol}</p><h1 className="mt-2 font-display text-3xl font-semibold">{item.company}</h1></div><div className="text-right"><p className="font-mono text-2xl font-semibold">{item.price.toLocaleString("vi-VN")} ₫</p><p className={`font-mono text-sm ${item.change >= 0 ? "text-bull" : "text-bear"}`}>{item.change >= 0 ? "+" : ""}{item.change}%</p></div></div><div className="mt-8 border-t border-slate-200 pt-6"><p className="label">AI research summary · synthetic</p><p className="mt-3 max-w-3xl leading-7 text-slate-700">{item.summary}</p></div><div className="mt-8 grid gap-px bg-slate-200 sm:grid-cols-3">{item.evidence.map(e=><div key={e.label} className="bg-slate-50 p-5"><p className="label">{e.label}</p><p className="mt-3 font-mono text-xl font-semibold">{e.value}</p><p className="mt-1 text-xs text-slate-500">{e.detail}</p></div>)}</div></div></div></main></>; }
